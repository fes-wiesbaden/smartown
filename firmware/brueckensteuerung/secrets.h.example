#pragma once

/**
 * Vorlage fuer die secrets.h Datei.
 * Kopiere diese Datei als 'secrets.h' und trage deine Zugangsdaten ein.
 * Die 'secrets.h' sollte NIEMALS auf GitHub hochgeladen werden!
 */

// WLAN-Konfiguration
constexpr char WIFI_SSID[] = "REPLACE_ME";
constexpr char WIFI_PASSWORD[] = "REPLACE_ME";

// MQTT-Broker-Konfiguration (IP deines Laptops)
constexpr char MQTT_HOST[] = "REPLACE_ME"; 
constexpr uint16_t MQTT_PORT = 1883;

// MQTT-Anmeldedaten (siehe Docker/Broker-Config)
constexpr char MQTT_USERNAME[] = "REPLACE_ME";
constexpr char MQTT_PASSWORD[] = "REPLACE_ME";
constexpr char MQTT_CLIENT_ID[] = "REPLACE_ME_UNIQUE_ID";
